import React,{useEffect,useState} from 'react';import {LineChart,Line,XAxis,YAxis,Tooltip,ResponsiveContainer} from 'recharts'
export default function Diagnostics({enabled=true}){const [runs,setRuns]=useState([]);useEffect(()=>{if(!enabled)return;fetch('/api/diagnostics').then(r=>r.json()).then(x=>setRuns(x.runs||[])).catch(()=>{})},[enabled]);const data=runs.map((r,i)=>({step:i,reward:r.metrics?.rollout_ep_rew_mean??r.metrics?.reward??0,loss:r.metrics?.policy_gradient_loss??0,entropy:r.metrics?.entropy_loss??0}));return <ResponsiveContainer width="100%" height={280}><LineChart data={data}><XAxis dataKey="step"/><YAxis/><Tooltip/><Line dataKey="reward" dot={false}/><Line dataKey="loss" dot={false}/><Line dataKey="entropy" dot={false}/></LineChart></ResponsiveContainer>}
